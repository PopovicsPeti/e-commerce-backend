service: authorization-service

plugins:
  - serverless-dotenv-plugin

provider:
  name: aws
  runtime: nodejs16.x  

functions:
  basicAuthorizer:
    handler: basicAuthorizer.handler
    environment:
      PopovicsPeti: ${env:TEST_PASSWORD}  

package:
  exclude:
    - .env
